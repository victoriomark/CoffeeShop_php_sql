const Review = () =>{    let image;    const create_btn = document.getElementById('create_btn');    const upload_img = document.getElementById('upload_img');    const Message = document.getElementById('Message');    // for file upload or input    upload_img.addEventListener('change',(event) =>{     image =  event.target.files[0]    })    create_btn.addEventListener('click',(e) =>{if(!image ||!Message.value || !e.target.value ){    alert("complete the form")}else {    const formData = new FormData();    formData.append('Message',Message.value);    formData.append('Image',image);    formData.append('CostumerId',e.target.value);    $.ajax({        url: '../Model/getReview.php',        type: 'post',        data: formData,        processData: false,        contentType: false,        success:function (data,status){            if(status === 'success'){                $('#Review_modal').modal('hide');                $('#Success_modal').modal('show');                setTimeout(() =>{                    location.reload()                },2000)            }        }    })}    })}Review();